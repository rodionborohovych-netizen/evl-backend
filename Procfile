# EVL Railway Deployment Configuration
# =====================================
# This Procfile tells Railway how to start the application

# Standard uvicorn command (Railway provides Python in PATH)
web: uvicorn main:app --host 0.0.0.0 --port $PORT
